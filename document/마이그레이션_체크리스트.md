# 프론트엔드 마이그레이션 체크리스트
> redesign/ → platform/ 마이그레이션 기준  
> 명세서 반영 완료 기준일: 2026-06-17

---

## 0. 기반 작업 (전체 페이지 공통)

### 0-1. 디자인 토큰 / CSS 변수
- [x] `globals.css`에 테마 CSS 변수 정의 (5개 테마 × 전체 토큰)
  - `worn-denim`, `slate-stage`, `crimson-amp`, `velvet-night`, `neon-moss`
  - `--background`, `--surface`, `--surface-elevated`, `--border`, `--border-subtle`
  - `--foreground`, `--muted-foreground`, `--subtle-foreground`
  - `--accent`, `--accent-hover`, `--accent-muted`, `--accent-foreground`
  - `--ok` / `--ok-bright` / `--bad` (상태 색상)
- [x] 폰트 토큰: `--font-display` (Anton), `--font-sans` (Space Grotesk), `--font-mono` (Space Mono), `--font-kr` (Noto Sans KR)
- [x] Google Fonts `preconnect` + `preload` 설정 (Anton, Space Grotesk, Space Mono, Noto Sans KR) — `next/font/google` 가 자동 처리
- [x] `color-scheme: dark` 전역 설정 (네이티브 폼 컨트롤 다크 처리)
- [x] number input 스피너 제거 전역 CSS
- [x] date/time picker 스타일 통일 CSS
- [x] custom scrollbar 스타일

### 0-2. 테마 시스템 (클라이언트 사이드)
- [x] `localStorage('theme')` 기반 테마 전환 훅 (`useTheme`)
- [x] `document.documentElement.setAttribute('data-theme', id)` 적용
- [x] 앱 최초 로드 시 저장된 테마 즉시 적용 (FOUC 방지 — `<script>` 인라인 또는 Next.js `beforeInteractive`)
> **참고**: 서버 저장 없음. localStorage 전용 클라이언트 기능.

### 0-3. 공유 컴포넌트
- [x] `ThemePicker` — 트리거(스와치 + 라벨 + 쉐브론) + 드롭다운 메뉴 (5개 테마 옵션)
- [x] `Kicker` — 모노 폰트, 왼쪽 액센트 선 포함 레이블
- [x] `Button` / `ButtonPrimary` / `ButtonGhost`
- [x] `Avatar` — 이니셜 자동 생성, 배경색 지정 가능
- [x] `Badge` / `BadgeAccent` / `BadgeLive` (pulse 애니메이션 포함)
- [x] `Chip` (multi-select, `aria-pressed` 토글)
- [x] `SegmentedControl` (학년 선택 등)
- [x] `Modal` / `Drawer` (모바일 sheet 변형 포함)
- [x] `AmbientBackground` — fixed radial gradient + hairline grid (랜딩·가입 전용)
- [x] 스크린 전환 애니메이션 (`screenIn` keyframe)

### 0-4. 레이아웃
- [x] `Header` — 로고, 데스크톱 Nav, 알림 벨, 프로필 메뉴
- [x] `BottomNav` — 모바일/PWA 고정 하단 내비 (홈·타임테이블·부원·팀·공지)
- [x] `AppMain` — 메인 콘텐츠 래퍼 (모바일에서 하단 내비 높이만큼 padding-bottom)
- [x] 반응형 브레이크포인트: 860px (데스크톱 Nav 숨김 + 하단 Nav 표시)

### 0-5. DB 마이그레이션
- [x] `users.school_year` 컬럼: `int` → `school_year_status` enum으로 변경
  - enum 값: `YEAR_1`, `YEAR_2`, `YEAR_3`, `YEAR_4`, `YEAR_5`, `COMPLETED`, `ON_LEAVE`, `GRADUATED`
- [x] `users.auth_key` 컬럼 추가: `text UNIQUE` (CSV 가져오기 시 자동 생성)
- [x] `reports` 테이블 신규 생성 (ERD 명세서 참고)
  - `report_category` enum: `BUG`, `OPINION`, `COMPLAINT`, `OTHER`
  - `report_status` enum: `PENDING`, `REVIEWED`, `RESOLVED`
- [x] RLS 정책: `reports` 테이블 (본인 제출 건 읽기, 운영진 전체 읽기/수정)

---

## 1. 랜딩 페이지 `/`

### UI
- [x] 헤더: 로고 + `ThemePicker` + "로그인" 버튼
- [x] Hero 섹션: kicker → `<h1>` (Anton, clamp) → 소개문 → CTA 버튼
- [x] CTA: 카카오 로그인 버튼 (노란 배경, 카카오 아이콘) + 보조 안내 텍스트
- [x] 통계 레일: 활동 부원 수 / 합주 팀 수 / 기수 / 정기 공연 수 (Anton 큰 숫자)
- [x] 활동 섹션: 카드 3개 (정기합주 / 정기공연 / 신입교육), 3열 → 모바일 1열
- [x] 모집 블록: 운영진 설정 기반 동적 렌더링

### 모집 블록 동적 처리
- [x] `GET /api/settings/recruitment` 호출 → `is_open`, `open_at`, `close_at` 반영
- [x] 모집 중: 학기명 / 우대 세션 태그 / 모집 기간 / "지원하기" 버튼 표시
- [x] 모집 마감: "모집 마감" 상태 + 잠금 아이콘 버튼 (비활성)
- [ ] 우대 세션, 헤드라인, 안내문구는 `GET /api/admin/settings/recruitment`의 확장 필드로 관리 (현재 미구현 필드 — 추후 추가)
> **현재 디자인**: 운영진 admin 화면에서 localStorage에 직접 저장하는 방식으로 프로토타입됨. 실제 구현에서는 API 사용.

### 접근 제어
- [x] 로그인 상태에서 `/` 접근 시 `/home` 리다이렉트 (상태에 따라)

---

## 2. 가입 방식 선택 (카카오 OAuth 콜백 후)

> **라우트**: `/link` 진입 전 분기 화면 — 별도 라우트(`/join`) 또는 `/link` 첫 화면으로 구현

### UI
- [x] 카카오 계정 칩: 아바타 이니셜 + 이름 + "카카오 계정으로 로그인됨" 라벨
- [x] 선택 카드 2개: "기존 부원" (primary 강조) / "신규 부원"
  - 각 카드: 아이콘 + 제목 + 설명 + 태그 목록 + "→" 하단 행
- [x] 하단: "잘 모르겠다면 기존 부원으로 먼저 확인해 보세요" + "다른 카카오 계정으로 로그인" 링크

### 로직
- [x] 카카오 OAuth 완료 후 `PENDING` 상태 유저가 이미 신청서를 제출했는지 확인
  - 신청서 있음 → `/apply` (가입 진행 상황 화면)
  - 신청서 없음 → 이 선택 화면 표시
- [x] OAuth 콜백에서 받은 카카오 프로필(이름) UI에 반영

---

## 3. 기존 부원 계정 연동 `/link`

### UI — Step 1 (인증키 입력)
- [x] 스텝 인디케이터: "인증키 확인" → "부원 정보 등록" (2단계)
- [x] 인증키 입력 필드: 모노 폰트, 대문자, `CL00-XXXX-XXXX` placeholder, 14자 제한
- [x] 입력 시 자동 포맷 (대소문자 무관 처리 — 서버에서 normalize)
- [x] 에러 상태: shake 애니메이션 + 빨간 테두리 + 에러 메시지
- [x] "인증키 확인" 버튼 (6자 미만 입력 시 disabled)
- [x] 안내 문구: "인증키는 회장·총무에게 발급받을 수 있어요"

### API 연동 (Step 1)
- [x] `POST /api/auth/link/search` — `{ auth_key }` 전송
- [x] 응답 200 → Step 2로 전환 (이름·기수 잠금 표시)
- [x] 응답 404 → 에러 UI (shake)
- [x] 응답 409 → "이미 연동된 인증키입니다" 메시지

### UI — Step 2 (부원 정보 등록)
- [x] 명단 확인 배너: 초록 체크 + "이름 · N기" 표시
- [x] 잠긴 필드 그리드: 이름 / 기수 (잠금 아이콘, 운영진만 수정 가능)
- [x] 프로필 섹션
  - [x] 프로필 사진 선택: "카카오 프로필" / "기본 이미지" 2개 옵션
  - [x] 닉네임 (선택, 20자)
  - [ ] 상태메시지 (선택, 60자, 글자 수 카운터) — DB 컬럼 미존재, 스킵
- [x] 음악 활동 섹션
  - [x] 세션 칩 (필수, 다중): 보컬·기타·베이스·드럼·건반
  - [x] 세션별 경력 연차 (선택): 세션 선택 시 숫자 입력 행 동적 생성
  - [x] 선호 장르 칩 (선택, 다중): 록·팝·인디·재즈·R&B·메탈·힙합·발라드·펑크·포크
- [x] 학적·연락처 섹션
  - [x] 학과 (필수)
  - [x] 학번 (필수, 숫자만)
  - [x] 학년 (필수): **1·2·3·4·5학년·수료** (6개 표시 — 휴학·졸업은 숨김)
  - [x] 연락처 (필수, 자동 하이픈 포맷)
  - [x] 연락처 비공개 토글
- [x] "연동하고 입장하기" 버튼

> ⚠️ **디자인 변경**: 카카오톡 ID 필드 없음 (디자인에는 있었으나 제거).

### API 연동 (Step 2)
- [x] `POST /api/auth/link/confirm` — `{ userId }` 전송 (Step 1에서 받은 user.id)
- [x] `PATCH /api/members/me` — 프로필 정보 저장 (세션, 학과, 학번, 학년, 연락처 등)
- [x] 완료 후 `/home` 리다이렉트 (기존 부원은 신청서 불필요)

### 접근 제어
- [ ] `PENDING` 상태만 접근 가능. 그 외 → `/home`

---

## 4. 신규 부원 지원서 + 가입 진행 상황 `/apply`

> 제출 전은 폼, 제출 후는 상태 화면으로 클라이언트 전환. URL(`/apply`) 유지.  
> 페이지 로드 시 기존 신청서 유무를 서버에서 확인해 초기 뷰 결정.

### UI — 지원서 폼
- [x] 01 프로필: 이름(실명, 필수) · 닉네임(선택)
- [x] 02 음악 활동
  - [x] 지원 세션 칩 (필수, 다중): 보컬·기타·베이스·드럼·건반
  - [x] 세션별 경력 연차 (선택, 동적)
  - [x] 선호 장르 칩 (선택, 다중): 록·팝·인디·재즈·R&B·메탈·발라드·포크
    > ⚠️ **디자인 변경**: "힙합", "펑크" 장르는 기존부원 폼에는 있으나 신규부원 폼에는 없음 — 통일 필요 (확인 후 결정)
- [x] 03 학적·연락처
  - [x] 학과(필수) / 학번(필수) / 학년(필수) / 연락처(필수)
  - [x] 학년: **1·2·3·4·5학년·수료·휴학·졸업** (전체 표시, 신규 지원자 대상)
  > ⚠️ **디자인 변경**: 카카오톡 ID 필드 없음.
- [x] 04 지원 동기: 자기소개 + 지원 동기 (필수, 200자, 글자 수 카운터)
- [x] 05 희망 면접 시간대: 날짜별 그룹핑 슬롯 목록, 복수 선택, 마감 슬롯 disabled
- [x] 제출 버튼 / 하단 안내: "제출 후에도 면접 시간대는 변경 요청 가능"
- [x] 유효성 검사: 모든 필수 필드 + 슬롯 1개 이상 선택 (없으면 빨간 outline)

### API 연동 (지원서 폼)
- [x] `GET /api/interview-slots` → 슬롯 목록 렌더링
- [x] `POST /apply` (Next.js Route Handler 또는 `/api/applications`) → 신청서 제출 (Supabase client 직접 upsert/insert)
- [x] `POST /api/interview-preferences` → 희망 슬롯 등록
- [x] 제출 완료 → 클라이언트에서 상태 화면으로 전환 (URL 유지)

### UI — 가입 진행 상황 화면 (같은 `/apply` 내)
- [x] kicker: "신규 부원 · 가입 현황"으로 변경
- [x] 3단계 스테퍼: 서류 확인 → 면접 → 결과
  - `doc`: 1번 active
  - `interview`: 1번 done / 2번 active
  - `await`: 1·2번 done / 3번 wait
  - `result`: pass/fail 색상 처리
- [x] 상태 Hero 배너 (색상/아이콘/메시지 단계별 변화)
- [x] 단계별 본문:
  - `doc`: 내가 고른 희망 면접 시간대 목록 + "면접 일정 변경하기" 버튼 (변경 폼 토글)
  - `interview`: 확정된 면접 일정 카드 (날짜·시간·유형)
  - `await`: 면접 진행 일정 카드 (음소거 스타일)
  - `result`: 합격/불합격 메시지
- [x] 지원 정보 요약 (이름, 지원 세션, 학과, 접수일시)

### API 연동 (상태 화면)
- [x] 페이지 로드 시 기존 신청서 조회 → 없으면 폼, 있으면 상태 화면
- [x] `GET /api/interview-preferences` → 희망 슬롯 표시
- [x] `POST /api/interview-preferences` → 희망 슬롯 변경 저장
- [ ] `storage` 이벤트 수신 불필요 — 실제 구현에서는 폴링 또는 실시간 구독으로 상태 갱신

### 접근 제어
- [x] `PENDING` 상태만 접근. 그 외 → `/home`

---

## 5. 홈 `/home`

### UI (3가지 변형 — 상태에 따라)
- [ ] 공통: kicker + 인사말 (`{닉네임 또는 이름}님, 안녕하세요`)
- [ ] **PROBATION**: 수습 현황 카드 (수습 시작일, 경과일, 수습 기간 안내)
- [ ] **ACTIVE/INACTIVE**: 내 팀 카드 + 다음 합주 일정 + 공지 미리보기
- [ ] **INTERVIEWING**: 면접 일정 카드 + 안내 배너

### API 연동
- [ ] `GET /api/members/me` → 내 프로필 및 상태 확인
- [ ] `GET /api/teams` → 내 팀 정보 (팀원 여부 필터)
- [ ] `GET /api/settings/recruitment` → 모집 기간 표시 (상단 배너)

### 접근 제어
- [ ] `INTERVIEWING` 이상 접근 가능

---

## 6. 타임테이블 `/timetable`

> **현재 상태**: 디자인 완성, API 미구현 (`/api/timetable/*` 없음)

### UI (우선순위: 높음 — 핵심 기능)
- [ ] 주간 그리드: 요일×시간 칸 렌더링
- [ ] 타임슬롯 예약 카드: 팀명 / 세션 요약 / 시간
- [ ] 예약 모달: 팀 선택, 시간 선택, 확정
- [ ] 기존 예약 수정 모달
- [ ] "지금 합주 중" 스트립 (현재 시간 기준 하이라이트)

### API 연동 (미구현 — 추후 작업)
- [ ] `GET /api/timetable` 엔드포인트 설계 및 구현 필요
- [ ] ERD에 `reservations` 테이블 추가 필요

### 접근 제어
- [ ] `INTERVIEWING` 이상 접근 가능

---

## 7. 부원 목록 `/members`

### UI
- [ ] 검색 입력 (이름/닉네임)
- [ ] 필터: 세션 칩 / 기수 드롭다운 / 역할 필터
- [ ] 부원 카드 그리드: 아바타 / 이름 / 기수 / 세션 뱃지 / 팀장 여부
- [ ] 화이트리스트 ★ 배지
- [ ] 운영진 카드 별도 섹션 (상단 or 하단)
- [ ] 페이지네이션

### 부원 프로필 모달 또는 `/members/[id]`
- [ ] 프로필 사진 / 이름 / 닉네임 / 기수 / 세션 / 장르 / 학과 / 학년
- [ ] 개인정보 마스킹 반영 (`privacy_settings` 기반)
- [ ] 팀 초대 버튼 (팀장/부팀장 전용)

### 내 프로필 `/members/me` 및 수정 `/members/me/edit`
- [ ] 수정 가능 필드: 닉네임, 상태메시지, 프로필사진, 세션, 장르, 학과, 학년, 연락처
- [ ] 학년: **1·2·3·4·5학년·수료** 표시 (휴학·졸업 숨김)
- [ ] 개인정보 공개 범위 설정 (field별 `all`/`member`/`admin`)

### API 연동
- [ ] `GET /api/members` (필터·페이지네이션)
- [ ] `GET /api/members/[id]`
- [ ] `PATCH /api/members/me`
- [ ] `POST /api/teams/[id]/invitations`

### 접근 제어
- [ ] 목록/상세: `PROBATION` 이상
- [ ] 내 프로필 수정: 본인만

---

## 8. 팀 `/teams`

### UI
- [ ] 팀 카드 목록: 팀명 / 연습곡 / 세션 구성 / 모집 상태 / 팀원 수
- [ ] "모집 중" 필터 토글
- [ ] 팀 상세 `/teams/[id]`: 팀 소개 / 팀원 목록 / 가입 신청 / 초대 목록
- [ ] 팀 생성 `/teams/new`
- [ ] 팀 수정 `/teams/[id]/edit`

### API 연동
- [ ] `GET /api/teams`, `POST /api/teams`
- [ ] `GET /api/teams/[id]`, `PATCH /api/teams/[id]`, `DELETE /api/teams/[id]`
- [ ] `POST /api/teams/[id]/join-requests`
- [ ] `PATCH /api/teams/[id]/join-requests/[requestId]`
- [ ] `POST /api/teams/[id]/leave`
- [ ] `DELETE /api/teams/[id]/members/[userId]`
- [ ] `GET /api/members/me/invitations`, `PATCH .../[invitationId]`
- [ ] `POST /api/teams/[id]/activate`

### 접근 제어
- [ ] 목록/상세: `PROBATION` 이상
- [ ] 생성: `ACTIVE` / `INACTIVE`
- [ ] 수정: 팀장/부팀장/운영진

---

## 9. 공지사항 `/notices`

> **현재 상태**: 디자인 완성, API 미구현

### UI
- [ ] 공지 목록: 제목 / 날짜 / 카테고리 뱃지 / 미리보기
- [ ] 공지 상세 `/notices/[id]`: 리치 텍스트 렌더링 (`notice-body` 스타일)
- [ ] 공지 작성 `/notices/new` (운영진 전용): contenteditable 리치 에디터 + 툴바
- [ ] 공지 수정 `/notices/[id]/edit` (운영진 전용)

### API 연동 (미구현 — 추후 작업)
- [ ] `GET /api/notices`, `POST /api/notices`
- [ ] `GET /api/notices/[id]`, `PATCH /api/notices/[id]`, `DELETE /api/notices/[id]`

### 접근 제어
- [ ] 조회: `INTERVIEWING` 이상
- [ ] 작성/수정/삭제: `ADMIN_ACCESS`

---

## 10. 알림 (헤더 벨 아이콘)

> **현재 상태**: 디자인 완성. FCM 연동은 별도 계획으로 관리됨 (memory: 알림 시스템 계획).

### UI
- [ ] 벨 아이콘 버튼 (미읽음 배지 숫자)
- [ ] 드롭다운 패널: 알림 목록 (카테고리 아이콘 / 제목 / 시간 / 읽음 여부)
- [ ] "모두 읽음" 버튼
- [ ] 알림 클릭 시 해당 화면으로 이동

### API 연동 (FCM 연동 완료 후)
- [ ] 알림 목록 및 읽음 처리 API (미구현)

---

## 11. 신고·제보 (프로필 메뉴 → 모달)

> 별도 라우트 없음. 프로필 메뉴에서 모달로 열림.

### UI
- [ ] 모달: 카테고리 선택 (버그 제보 / 의견 건의 / 부원 관련 / 기타)
- [ ] 제목 입력 (필수)
- [ ] 내용 입력 textarea (필수)
- [ ] 익명 제출 토글 (기본 비익명)
- [ ] 제출 완료 시 성공 메시지

### API 연동
- [ ] `POST /api/reports` — `{ category, title, body, is_anonymous }`

---

## 12. 운영진 — 모집 관리 `/admin` (settings 탭)

### UI
- [ ] 탭바: 모집 설정 / 신청서 / 면접 슬롯
- [ ] 모집 상태 토글 (열기/닫기)
- [ ] 모집 기간 날짜 선택 (open_at / close_at)
- [ ] 헤드라인, 안내 문구, 우대 세션 태그 편집
- [ ] 랜딩 페이지 미리보기 패널 (우측 sticky, 980px 이하 숨김)
- [ ] 저장 바 (sticky bottom)

### API 연동
- [ ] `GET /api/admin/settings/recruitment`
- [ ] `PATCH /api/admin/settings/recruitment`

---

## 13. 운영진 — 신청서 관리 `/admin/applications`

### UI
- [ ] 신청서 카드 목록: 이름 / 세션 / 학과 / 지원 동기 요약 / 상태 뱃지
- [ ] 상태 필터: 전체 / 서류 검토 / 면접 예정 / 합격 / 불합격
- [ ] 신청서 상세 (카드 클릭 또는 서브페이지): 지원 정보 전체 / 희망 슬롯 목록
- [ ] 슬롯 배정 (`schedule`) / 결과 입력 (`result`) 액션

### API 연동
- [ ] `GET /api/admin/applications/[id]/preferences`
- [ ] `POST /api/admin/applications/schedule`
- [ ] `POST /api/admin/applications/result`
- [ ] `GET /api/admin/interview-slots`, `POST ...`, `DELETE .../[id]`

---

## 14. 운영진 — 부원 관리 `/admin/members`

### UI
- [ ] 테이블: 체크박스 / 이름·닉네임 / 기수 / 세션 / 학과 / 학년 / 상태 / 역할 / 경고 수 / 상세 아이콘
- [ ] 가로 스크롤 (모바일)
- [ ] 체크박스 다중 선택 + 하단 액션 바 (일괄 상태 변경)
- [ ] 우측 Drawer: 부원 상세 + 상태 변경 + 경고 발급 + 탈퇴 처리
- [ ] Toggle switch: 화이트리스트 / 역할 변경

### API 연동
- [ ] `GET /api/admin/members/link-status`
- [ ] `PATCH /api/admin/members/[id]`
- [ ] `POST /api/admin/members/transition`
- [ ] `GET /api/admin/members/[id]/warnings`, `POST ...`
- [ ] `POST /api/admin/import` (CSV 업로드, auth_key 반환 처리)

---

## 15. 운영진 — 신고·제보 관리

### UI
- [ ] 신고 목록: 카테고리 / 제목 / 제보자(익명 시 "익명") / 접수일 / 상태
- [ ] 상태 필터 (PENDING / REVIEWED / RESOLVED)
- [ ] 카테고리 필터
- [ ] 상세 보기 + 처리 상태 변경 + 운영진 메모 저장

### API 연동
- [ ] `GET /api/admin/reports`
- [ ] `PATCH /api/admin/reports/[id]`

---

## 16. 접근 제어 미들웨어

- [ ] `middleware.ts`: 라우트별 상태 검증 (IA 명세서 섹션 3 기준)
- [ ] `PENDING` + 모집 기간 아님 → `/status?reason=not_open`
- [ ] 미인증 → `/` (랜딩)
- [x] `PENDING` + 신청서 없음 → `/link` or 가입 방식 선택 화면 (랜딩 페이지 `/` 진입 시 처리됨 — middleware 미구현)
- [x] `PENDING` + 신청서 있음 → `/apply` (상태 화면) (랜딩 페이지 `/` 진입 시 처리됨 — middleware 미구현)

---

## 17. PWA 설정

- [ ] `manifest.json`: 앱 이름, 아이콘, `display: standalone`, `theme_color: #151A26`
- [ ] `apple-mobile-web-app-*` 메타 태그 (모든 페이지)
- [ ] `viewport-fit: cover` + `env(safe-area-inset-bottom)` 하단 여백 처리

---

## 우선순위 요약

| 순위 | 항목 |
|------|------|
| 🔴 즉시 | 0. 기반 작업 (토큰·컴포넌트·DB 마이그레이션) |
| 🔴 즉시 | 1. 랜딩 페이지 |
| 🔴 즉시 | 3. 기존 부원 연동 (인증키 방식) |
| 🔴 즉시 | 4. 신규 부원 지원서 + 가입 진행 상황 |
| 🟠 높음 | 5. 홈 |
| 🟠 높음 | 6. 타임테이블 (API 설계 병행 필요) |
| 🟠 높음 | 7. 부원 목록 / 프로필 |
| 🟡 중간 | 8. 팀 |
| 🟡 중간 | 12~14. 운영진 관리 화면들 |
| 🟢 낮음 | 9. 공지사항 (API 미구현) |
| 🟢 낮음 | 10. 알림 (FCM 연동 후) |
| 🟢 낮음 | 11. 신고·제보 |
| 🟢 낮음 | 15. 운영진 신고 관리 |
